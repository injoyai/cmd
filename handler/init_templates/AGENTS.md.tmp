# {{.ModuleName}}

## 项目概述

{{.ModuleName}} 是一个基于 Go 语言的标准服务项目,遵循 Go 社区最佳实践和项目布局规范。

## 技术栈

| 层次 | 技术 | 版本 |
|------|------|------|
| 语言 | Go | {{.GoVersion}} |
| 配置管理 | [github.com/injoyai/conv/cfg](https://github.com/injoyai/conv) | v1.2.5 |
| 构建 | Go Modules + build.sh | — |
| 容器化 | Docker (多阶段构建) | — |

## 目录结构与约束

```
.
├── bin/                  # 编译产物目录 (gitignored)
├── config/
│   └── config.yaml       # 主配置文件,由 conv/cfg 默认加载
├── cmd/                  # (可选) 多入口点命令目录
├── internal/             # 内部包目录 (Go 编译器强制执行)
│   └── app/              # 业务逻辑层
│   └── pkg/              # 内部共享库
├── pkg/                  # (可选) 外部可导入包
├── scripts/
│   └── build.sh          # 交叉编译脚本 (多平台)
├── .gitignore
├── AGENTS.md             # AI 助手约束文件 (本文件)
├── Dockerfile
├── go.mod
├── go.sum
├── main.go               # 应用入口
└── README.md
```

**目录约束规则:**
- `bin/`: 仅存放编译产物,不提交源码,已加入 `.gitignore`
- `internal/`: 包路径强制私有化(Go 编译器级别),外部项目无法 import
- `config/`: 配置模板与运行配置,不提交敏感信息
- `cmd/` 与 `internal/` 遵循 [golang-standards/project-layout](https://github.com/golang-standards/project-layout)

## 编码规范

### Go 语言约定

- **导入顺序**: 标准库 → 第三方包 → 内部包,每组空行分隔
- **错误处理**: 不忽略错误返回值,使用 `fmt.Errorf("context: %w", err)` 包裹错误链
- **命名**:
  - 接口名: `-er` 后缀 (`Reader`, `Writer`, `Handler`)
  - 私有函数: 小写驼峰
  - 导出函数: 大写驼峰
  - 包名: 小写单数形式
- **零值初始化**: `var t Type` 而非 `t := Type{}`
- **配置读取**: 统一使用 `github.com/injoyai/conv/cfg`,不使用 `os.Getenv` 直接读取

### 文件头模板

```go
package main

import (
	"context"
	"fmt"
	"os"

	"github.com/injoyai/conv/cfg"
)
```

## 配置管理

配置文件 `config/config.yaml` 通过 `github.com/injoyai/conv/cfg` 自动加载。

### 标准用法

```go
// 默认加载: ./config/config.yaml + 环境变量
appName := cfg.GetString("app.name", "默认值")
serverPort := cfg.GetInt("server.port", 8080)
debugMode := cfg.GetBool("debug", false)
```

### 配置说明

- `cfg.Default`: 默认加载 `./config/config.yaml`(YAML) + 环境变量
- `cfg.Init(...)`: 自定义配置源,优先级按传入顺序
- 默认值参数: `cfg.GetXxx(key, defaultValue)` — key 不存在时返回默认值
- 嵌套读取: `cfg.GetString("app.name")` 对应 YAML 中的 `app.name`

### 配置示例 (`config/config.yaml`)

```yaml
app:
  name: {{.ModuleName}}
  version: 0.0.1

server:
  port: 8080
  host: 0.0.0.0

log:
  level: info
  format: json
```

## 构建与运行

```bash
# 本地运行
go run main.go

# 编译到 bin/
go build -o bin/{{.ModuleName}} .

# 多平台交叉编译
bash scripts/build.sh

# Docker 构建
docker build -t {{.ModuleName}} .

# 依赖治理
go mod tidy
```

## AI 助手约束

本文件供 AI 编码助手读取,执行代码生成与修改时必须遵守以下规则:

1. **不修改 `AGENTS.md` 文件本身**,除非用户明确要求
2. **遵循现有代码风格**: 使用项目中已经建立的模式(如 `conv/cfg` 读取配置,而非 `os.Getenv`)
3. **不改变项目结构**: 不移动已存在的目录和文件,新增文件遵循目录约束规则
4. **不破坏向后兼容性**: 修改导出函数/类型时保留原有签名或者提供兼容层
5. **最小变更原则**: 只修改与需求直接相关的代码,不进行"顺手"的重构或格式化
6. **配置变更同步**: 修改配置相关代码时同步更新 `config/config.yaml` 模板
7. **依赖管理**: 新增依赖后必须运行 `go mod tidy`
8. **错误处理**: 所有可能返回 error 的调用必须处理返回值,不允许 `_ = foo()` 跳过
